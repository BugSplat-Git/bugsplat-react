import { BugSplat } from 'bugsplat';
import { createScope } from './scope';

/**
 * Default scope for managing `BugSplat` instance
 */
const defaultScope = createScope(BugSplat);

export default defaultScope;
